[tool.black]
line-length = 88
py36 = true
include = '\.pyi?$'
exclude = '''
/(
    \.gitignore
  | \.git
  | ui
  | terraform
  | builddir

  # The following are specific to Black, you probably don't want those.
  | blib2to3
  | tests/data
)/
'''
